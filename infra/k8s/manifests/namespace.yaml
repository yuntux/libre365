apiVersion: v1
kind: Namespace
metadata:
  name: libre365
  labels:
    app.kubernetes.io/part-of: libre365
    # Study chapter 4.4: all containerized components (except Grommunio, Proxmox VM,
    # see 4.3) are deployed in Kubernetes from the initial 100-user target.
    environment: production

---
# Ansible Galaxy collections required by the playbooks/roles in this directory.
# Install with: ansible-galaxy collection install -r requirements.yml
collections:
  - name: community.general
    version: ">=8.0.0,<10.0.0" # grommunio.yml: community.general.ufw

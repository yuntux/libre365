{
  "realm": "libre365",
  "enabled": true,
  "displayName": "libre365 (dev)",
  "sslRequired": "none",
  "registrationAllowed": false,
  "loginTheme": "keycloak",
  "accessTokenLifespan": 3600,
  "roles": {
    "realm": [
      { "name": "consultant", "description": "Standard libre365 user (study 1.7)" },
      { "name": "admin", "description": "libre365 administration" }
    ]
  },
  "clients": [
    {
      "clientId": "libre365-integration-tests",
      "name": "Suite de tests d'integration (tests/integration)",
      "description": "OIDC client used by the pytest suite to obtain end-to-end SSO tokens against each building block (study 4.5, 5.5).",
      "enabled": true,
      "protocol": "openid-connect",
      "publicClient": false,
      "clientAuthenticatorType": "client-secret",
      "secret": "devonly-changeme-client-secret",
      "standardFlowEnabled": true,
      "directAccessGrantsEnabled": true,
      "serviceAccountsEnabled": true,
      "redirectUris": ["http://localhost/*"],
      "webOrigins": ["*"]
    }
  ],
  "users": [
    {
      "username": "testuser",
      "enabled": true,
      "emailVerified": true,
      "email": "testuser@libre365.localhost",
      "firstName": "Test",
      "lastName": "User",
      "credentials": [
        {
          "type": "password",
          "value": "devonly-changeme-testuser",
          "temporary": false
        }
      ],
      "realmRoles": ["consultant"]
    }
  ]
}

# Dépendances de la suite de tests d'intégration (étude section 4.5 "Rejeu des scénarios de test").
# Installer avec: pip install -r tests/integration/requirements.txt

pytest>=7.4,<9
pytest-timeout>=2.2,<3          # timeouts explicites par test (voir contrainte: échec propre si la stack n'est pas démarrée)
pytest-html>=4.1,<5             # rapport de résultats succès/échec par scénario (étude 4.5, ligne 797)
requests>=2.31,<3
responses>=0.25,<1              # mock HTTP pour les tests connecteurs en boîte noire (test_connectors.py)
PyJWT>=2.8,<3                   # génération du JWT de config OnlyOffice Document Server
python-keycloak>=3.9,<4         # client OIDC pour le grant "password" (fixture keycloak_token)

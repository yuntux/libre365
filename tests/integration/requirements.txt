# Dependencies for the integration test suite (study section 4.5 "Replaying test scenarios").
# Install with: pip install -r tests/integration/requirements.txt

pytest>=7.4,<9
pytest-timeout>=2.2,<3          # explicit per-test timeouts (see constraint: fail cleanly if the stack isn't up)
pytest-html>=4.1,<5             # pass/fail results report per scenario (study 4.5, line 797)
requests>=2.31,<3
responses>=0.25,<1              # HTTP mocking for black-box connector tests (test_connectors.py)
PyJWT>=2.8,<3                   # generates the OnlyOffice Document Server config JWT
beautifulsoup4>=4.12,<5          # parses Keycloak's login form for test_sso_e2e.py's real
                                 # browser-redirect login flow (no JS needed: Keycloak's
                                 # default theme is a plain HTML form POST)
